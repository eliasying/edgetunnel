{
	"name": "edgetunnel",
	"version": "1.0.0",
	"description": "VLESS-over-WebSocket proxy worker for Cloudflare Workers. Community fork of zizifn/edgetunnel.",
	"private": true,
	"scripts": {
		"dev": "wrangler dev src/worker-vless.js",
		"dev-socks5": "wrangler dev src/worker-with-socks5-experimental.js",
		"deploy": "wrangler deploy"
	},
	"license": "GPL-2.0-only",
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20230518.0",
		"ip-cidr": "^3.1.0",
		"wrangler": "^3.1.0"
	}
}
